const IconContact = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className="group cursor-pointer transition duration-300 ease-in-out rounded-full p-1.5 bg-transparent hover:bg-white">
      {children}
    </div>
  );
};

export default IconContact;
